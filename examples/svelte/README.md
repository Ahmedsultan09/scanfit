# Svelte example

This example maps the framework-independent session snapshot into Svelte state and disposes the worker when the component unmounts.

From the repository root:

```sh
npm run build
npm run dev --workspace @scanfit/example-svelte
```
