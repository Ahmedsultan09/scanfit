# Vite + React example

This example uses the lightweight `ScannerTrigger`, lazy-loads ScanFit's complete React workflow and receives the confirmed PDF through `onComplete`.

From the repository root:

```sh
npm run build
npm run dev --workspace @scanfit/example-vite-react
```

Open the URL printed by Vite. No backend or document upload is involved.
