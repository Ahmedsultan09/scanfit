# Vue example

This example connects the framework-independent session API to Vue state. It demonstrates lifecycle cleanup and exact-size result handling without wrapping the React component.

From the repository root:

```sh
npm run build
npm run dev --workspace @scanfit/example-vue
```
