# Vanilla TypeScript example

This example uses `createScanSession` without React. It demonstrates import, progress, the exact-byte export result and cleanup; a custom editor can use the remaining session operations.

From the repository root:

```sh
npm run build
npm run dev --workspace @scanfit/example-vanilla
```
